package com.ibramir.busstation.station.vehicles;

import com.ibramir.busstation.station.Trip;

public class Bus extends Vehicle {
    public Bus(Trip assignedTrip, int maxSeats) {
        super(assignedTrip, maxSeats);
    }
}
